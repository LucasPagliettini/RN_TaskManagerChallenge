import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 15,
  },
  text: {
    color: 'black',
    marginBottom: 5,
  },
});

export default styles;
